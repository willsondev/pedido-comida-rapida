<template>
    <!-- Contenido HTML del componente -->
    <div>
      <h1>Menu</h1>
    </div>
  </template>
  
  <script>
  export default {
    name: 'MiComponente', // Nombre del componente
    
    data() {
      return {
        // Datos locales del componente (si los necesitas)
      };
    },
    methods: {
      // Métodos del componente (si los necesitas)
    },
    computed: {
      // Propiedades computadas del componente (si las necesitas)
    },
    // Otras opciones del componente (si es necesario)
  };
  </script>
  
  <style scoped>
  /* Estilos CSS específicos del componente (si es necesario) */
  </style>
  